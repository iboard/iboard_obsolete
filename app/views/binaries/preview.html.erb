<%= @binary.embed(original_binary_path(@binary),(params[:width].to_i), (params[:height].to_i-10) ) -%><br/>
<font size=-2><br/>
  <%= link_to @binary.title, @binary %><br>
  <%= @binary.mime_type -%>, <%= @binary.size/1024 %>kb
</font>