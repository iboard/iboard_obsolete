<h1><%= _('New page') -%></h1>

<%= error_messages_for :page %>

<% form_for(@page) do |f| %>
  <%= render :partial => 'fields', :locals => { :f => f } -%>
  <p>
    <%= f.submit _("Create") %>
  </p>
<% end %>

<%= link_to _('Back'), pages_path %>
