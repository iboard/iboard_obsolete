<% @events.each do |event| %>
  <%= "#{request.env['SERVER_NAME']};#{event.title};#{event.begins_at.to_s(:db)};#{event.price};#{event.price_prebooking};"+
      "#{event.price_reduced};#{link_to( 'Details', event_url(event) )}\n" + 
      block_ascii_text(event.body,60,'		') 
  %>
<% end %>

