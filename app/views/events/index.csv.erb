<% @events.each do |event| %>
  <%= "#{event.title};#{event.begins_at.to_s(:db)};#{event.price};#{event.price_prebooking};#{event.price_reduced};#{event.body.gsub(/\n/,'<br>')};" +
      link_to( 'Details', event_url(event) ) %>
<% end %>